[theme]
primaryColor = "#F58518"
backgroundColor = "#0D1117"
secondaryBackgroundColor = "#161B22"
textColor = "#E6EDF3"
font = "sans serif"

# Note: Custom font (Inter) is loaded via CSS in the app

[server]
headless = true
